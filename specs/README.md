# Specification

## Data structure

## Operators

    Table.insert(Row)

    Table.find(column, value) -> Vec<Row>
    Index.find(value) -> Vec<Row>

    Table.remove(column, value)
    Index.remove(value)
